import { Button } from '../../../index';
import { Typography } from '@material-ui/core/';

export const SignAccount = () => {
    return <>
        <Typography component={'h2'}>Active Account</Typography>
        <Button onClick={() => {
            //TODO
        }}>Click Approve</Button></>
}
